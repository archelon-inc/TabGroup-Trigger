# Chrome Web Store 説明文（日本語）

## 短い説明（128文字以内）

```
URL経由でChromeのタブグループを切り替える拡張機能。キーボードショートカットユーティリティと組み合わせて、ホットキーで瞬時にグループ切り替えが可能。
```

文字数: 78文字

## 詳細な説明

```
ChromeのタブグループをURL経由で切り替える拡張機能です。キーボードショートカットユーティリティ（BetterTouchTool、AutoHotkeyなど）と組み合わせることで、お好みのホットキーでタブグループを瞬時に切り替えできます。

【なぜこの拡張機能？】
Chromeにはタブグループを切り替えるためのネイティブなキーボードショートカット機能がありません。この拡張機能は、URL経由でタブグループの切り替えをトリガーできるようにすることで、この問題を解決します。例えば Cmd+1 で「仕事」グループに、Cmd+2 で「プライベート」グループに瞬時にジャンプできるようになります。

【主な機能】
✓ キーボードショートカット連携
  キーボードショートカットユーティリティとシームレスに連携し、ホットキーベースのタブグループ切り替えを実現

✓ URLベースのトリガー
  https://extension.tabgroup-trigger/{グループ名} にアクセスすると、指定したタブグループに即座に切り替わります

【使い方】
1. Chromeでタブグループを作成し、名前を付けます
2. キーボードショートカットユーティリティで、URLを開くアクションを設定します
   例: Cmd+Shift+1 → https://extension.tabgroup-trigger/仕事
3. 設定したホットキーを押すと、瞬時にタブグループが切り替わります

【推奨ツール】
• macOS: BetterTouchTool、Keyboard Maestro
• Windows: AutoHotkey、PowerToys（バージョン 0.79 以降）

【セットアップ例（BetterTouchTool）】
1. 新しいキーボードショートカット（例: Cmd+Shift+1）を作成
2. アクションを「Open URL」に設定
3. URL を入力: https://extension.tabgroup-trigger/仕事
4. これで Cmd+Shift+1 を押すと「仕事」タブグループに即座に切り替わります

【ヒント】
• 拡張機能は各グループで最後にアクティブだったタブを記憶します
• グループに切り替えると、最後に見ていたタブがアクティブになります
• タブ履歴がない場合は、グループの最初のタブがアクティブになります

【制限事項】
• 保存されたタブグループ（全タブが削除されグループラベルのみ残る状態）には移動できません
  注：折りたたまれたタブグループ（タブは非表示だが削除されていない）は正常に動作します
• グループ名に特殊文字（?、/、#、%など）が含まれる場合、URLエンコードが必要です
  例: 「質問?回答」→ https://extension.tabgroup-trigger/%E8%B3%AA%E5%95%8F%3F%E5%9B%9E%E7%AD%94
• 25回以上連続でタブグループ移動を行うと、Chromeのセッション履歴制限により、それより前のタブが Cmd+Shift+T で復活できなくなる可能性があります

【プライバシー】
この拡張機能はデータを一切収集しません。すべての処理はローカルで完結します。

【サポート】
問題が発生した場合やご要望がある場合は、GitHubリポジトリのIssuesからご報告ください。
https://github.com/archelon-inc/TabGroup-Trigger/issues
```
